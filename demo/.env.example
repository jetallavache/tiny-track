# TinyTrack Demo — environment variables
# Copy to .env.local and fill in your values

# WebSocket URL of the tinytrack gateway (ws:// or http:// — both work)
# Default: ws://localhost:25015
VITE_WS_URL=ws://localhost:25015

# Auth token (leave empty if auth is disabled on the server)
VITE_WS_TOKEN=
